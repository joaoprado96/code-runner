docker pull hashicorp/vault:latest

docker run --cap-add=IPC_LOCK -e 'VAULT_DEV_ROOT_TOKEN_ID=myroot' -e 'VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200' -p 8200:8200 --name=vault hashicorp/vault

http://localhost:8200

Chave privada
openssl genrsa -out key.pem 2048

Certificado assinado:
openssl req -new -x509 -sha256 -key key.pem -out cert.pem -days 365



vault login <Token>

vault kv put <caminho_do_secreto> username=<usuario> password=<senha>

vault kv put secret/myapp/database username="dbuser" password="dbpassword"

vault kv get secret/myapp/database
